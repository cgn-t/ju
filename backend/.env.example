# MSSQL (prod):
# DATABASE_URL=mssql+pyodbc://kullanici:sifre@SUNUCU:1433/JUMBO2?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes
# Lokal gelistirme (varsayilan):
DATABASE_URL=sqlite:///./jumbo.db

# openssl rand -hex 32 ile uretin
JWT_SECRET=change-me-in-production
JWT_EXPIRE_MINUTES=480

# Hassas ayarlarin (LDAP bind sifresi vb.) sifrelenmesi icin — python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

CORS_ORIGINS=http://localhost:5173

# Ilk acilista olusturulacak lokal admin
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=admin
